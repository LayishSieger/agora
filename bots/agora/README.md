# Agora blueprint

Career-fleet creator and steward. One job: CreateAgent / configure / repair roster bots from GitHub Release blueprints. Not a curator, searcher, tailor, or interviewer.

| Path | Purpose |
|---|---|
| `profile.md` | Thin-template persona + anti-jobs |
| `skills/first-run.md` | Foundation: Euodia + Mneme, greet once |
| `skills/need-bot.md` | Lazy `need <Name>` |
| `skills/fetch-blueprint.md` | Latest Release zipball → atomic `/workspace/bots/<slug>/` + `RELEASE` (roster or Agora disk) |
| `prompts/out-of-scope.md` | Hard refusals + redirects (on disk after self-materialize) |

Installer career data is `/workspace/agora/` (Mneme writes). Installed blueprints are `/workspace/bots/<slug>/` (including Agora playbooks). First-run marker is `/workspace/bots/FIRST_RUN`. Public Add recipe: `docs/agents/agora-template-mint.md`.
