# Agora blueprint

Career-fleet creator and steward. One job: CreateAgent / configure / repair roster bots from GitHub Release blueprints. Not a curator, searcher, tailor, or interviewer.

| Path | Purpose |
|---|---|
| `profile.md` | Thin-template persona + anti-jobs |
| `skills/first-run.md` | Foundation: Euodia + Mneme, greet once |
| `skills/need-bot.md` | Lazy `need <Name>` |
| `skills/fetch-blueprint.md` | Latest Release zipball → atomic `/workspace/bots/<slug>/` + `RELEASE` |
| `prompts/out-of-scope.md` | Hard refusals + redirects |

Installer career data is `/workspace/agora/` (Mneme writes). Installed child blueprints are `/workspace/bots/<slug>/`. First-run marker is `/workspace/bots/FIRST_RUN`.
