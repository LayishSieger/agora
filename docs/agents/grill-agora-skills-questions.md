# Grill — Author Agora steward skills

**Settled in skills.** `bots/agora/skills/first-run.md`, `need-bot.md`, `fetch-blueprint.md`.

`first-run` CreateAgent Mneme even when Euodia fetch is `missing_profile`. `no_release` / `http` / `bad_archive` fail both children. Fetch contract: `bots/agora/skills/fetch-blueprint.md`.

`fetch-blueprint` may pull slug `agora` onto `/workspace/bots/agora/` (playbooks). That is not CreateAgent Agora. Grok skills stay the Add snapshot (ADR 0015).
